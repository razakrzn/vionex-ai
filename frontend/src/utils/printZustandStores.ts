/**
 * Utility function to print all Zustand store data to console
 * Useful for debugging and checking store state
 */

import { useAuthStore } from "@/stores/authStore";
import { useAdminStore } from "@/stores/adminStore";
import { useEmiratesStore } from "@/stores/emiratesStore";
import { useThemeStore } from "@/stores/themeStore";
import { usePartnerStore } from "@/stores/partnerStore";
import { useLanguageStore } from "@/stores/languageStore";

export const printAllZustandStores = () => {
  // Logging disabled - only login-related logs are kept
};

/**
 * Print specific store data
 */
export const printAuthStore = () => {
  // Logging disabled - only login-related logs are kept
};

export const printAdminStore = () => {
  // Logging disabled - only login-related logs are kept
};

export const printEmiratesStore = () => {
  // Logging disabled - only login-related logs are kept
};

export const printThemeStore = () => {
  // Logging disabled - only login-related logs are kept
};

export const printPartnerStore = () => {
  // Logging disabled - only login-related logs are kept
};

export const printLanguageStore = () => {
  // Logging disabled - only login-related logs are kept
};

