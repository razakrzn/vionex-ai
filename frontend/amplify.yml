version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
customRules:
  - source: https://www.vionex-ai.com/<*>
    target: https://vionex-ai.com/<*>
    status: '301'
  - source: </^[^.]+$|\.(?!(css|gif|ico|jpg|js|png|txt|svg|woff|woff2|ttf|map|json|webp|xml|mp4|webm|ogg)$)([^.]+$)/>
    target: /index.html
    status: '200'

